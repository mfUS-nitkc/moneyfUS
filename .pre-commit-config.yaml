repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  # Ruff version.
  rev: v0.7.4
  hooks:
    # Run the linter.
    - id: ruff
      args: [ --fix ]
      files: '^backend/.*\.py'
    # Run the formatter.
    - id: ruff-format
      files: '^backend/.*\.py'
